# Copiar a .env y ajustar (docker compose carga .env automáticamente).
# La contraseña del rol `keycloak` en PostgreSQL está fijada en init/postgres/01-init.sql;
# si cambias KEYCLOAK_DB_PASSWORD aquí, debes alinear la misma contraseña en ese SQL y recrear el volumen de Postgres.

# PostgreSQL (BD aplicación: mtl; esquemas catalog, media, notification, ai + PostGIS)
POSTGRES_USER=mtl
POSTGRES_PASSWORD=mtl_dev_password
POSTGRES_DB=mtl
# Puerto en el host (mapeo host→contenedor). Por defecto 5433 para no chocar con un PostgreSQL local en 5432.
POSTGRES_PORT=5433

# MongoDB
MONGO_INITDB_ROOT_USERNAME=mtl
MONGO_INITDB_ROOT_PASSWORD=mtl_mongo_dev_password
MONGO_PORT=27017

REDIS_PORT=6379

MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=minio_dev_password
# Orígenes permitidos para PUT/GET desde el navegador (CORS global en MinIO community). Opcional: por defecto en compose son Vite 5173.
# MINIO_API_CORS_ALLOW_ORIGIN=http://localhost:5173,http://127.0.0.1:5173
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001
# URL pública de MinIO para presign (navegador). En Windows+Docker usar 127.0.0.1, no localhost (IPv6).
# MINIO_SERVER_URL=http://127.0.0.1:9000
# MTL_MEDIA_STORAGE_PUBLIC_ENDPOINT=http://127.0.0.1:9000

# Kafka: clientes en el host usan el puerto EXTERNAL (9094); dentro de Docker usan kafka:9092
KAFKA_PORT_INTERNAL=9092
KAFKA_PORT_HOST=9094

# Mailpit: SMTP local sin salida a Internet (pruebas de correo, HU-007). UI web en MAILPIT_UI_PORT.
# notification-service en el host: spring.mail.host=localhost, spring.mail.port=MAILPIT_SMTP_PORT
MAILPIT_SMTP_PORT=1025
MAILPIT_UI_PORT=8025

# Keycloak: modo start-dev solo desarrollo (ver infra/compose/README.md). Realm importado: mtl; cliente SPA: mtl-spa; issuer típico http://localhost:8180/realms/mtl (ajustar puerto si KEYCLOAK_PORT cambia).
KEYCLOAK_PORT=8180
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin_dev_password
KEYCLOAK_DB_USER=keycloak
KEYCLOAK_DB_PASSWORD=keycloak_dev_password

# Observabilidad (Prometheus + Grafana, ADR-0005). Microservicios en el host en 8080-8084.
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin_dev_password

# Imagenes de aplicacion (docker-compose.apps.yml). Tag local por defecto; preparado para CI futuro.
MTL_IMAGE_TAG=local

# Frontend dockerizado (Nginx). Puerto libre respecto a gateway 8080-8084 y Vite 5173.
FRONTEND_PORT=8088
