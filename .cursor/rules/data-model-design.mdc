---
description: Reglas para diseño de modelos de datos y entidades persistentes
globs:
  - "services/**/domain/**/*.java"
  - "services/**/entity/**/*.java"
  - "services/**/model/**/*.java"
alwaysApply: false
---

# Data model design rules

## Objetivo
Diseñar modelos de datos simples, consistentes, extensibles y adecuados para un sistema colaborativo de catálogo de árboles con fotos, ubicación, notificaciones y roles.

## Reglas
- Diseña primero desde el dominio y los casos de uso, no desde la base de datos.
- Usa nombres en español de entidades y atributos expresivos, en singular y alineados con el lenguaje del negocio.
- **Ubicación en código y API:** paquetes (`domain`, repositorios, proyecciones, `controller`, `dto`, sin exponer entidades JPA en REST) según [spring-boot-4-backend.mdc](spring-boot-4-backend.mdc) (*Plantilla de paquetes* y *Persistencia*).
- Separa claramente:
  - entidades persistentes,
  - DTOs de entrada/salida,
  - objetos de dominio auxiliares,
  - enums.
- Evita entidades anémicas con campos ambiguos o genéricos como `value`, `data`, `info`, `type1`.

## Modelado
- Prefiere relaciones simples y explícitas.
- Usa `@ManyToOne` y `@OneToMany` solo cuando aporten claridad real.
- Evita relaciones bidireccionales innecesarias.
- No modeles una relación `@ManyToMany` directa si puede crecer en funcionalidad; crea una entidad intermedia.
- Usa claves primarias numéricas estables.
- Añade restricciones de unicidad e índices cuando el caso de uso lo requiera.
- Modela campos obligatorios como no nulos.
- Para catálogos estables usa `enum`; para catálogos evolutivos usa entidad propia.

## Dónde persistir (`catalog-service`)

- **PostgreSQL** (esquema `catalog`): maestros transaccionales — `especie`, `ejemplar`, taxonomía, ubicación PostGIS, integridad referencial.
- **MongoDB** (`especie_detalle`, `ejemplar_detalle`): enriquecimientos semiestructurados y observaciones; **no** añadirlos como columnas JPA ni como entidad `@Document` sustituta del maestro SQL.
- Canónico del híbrido: [mongo-hybrid.mdc](mongo-hybrid.mdc), [mongo.md](../../docs/data-model/mongo.md). Ante duda «¿SQL o Mongo?», consultar ahí — no duplicar criterios en esta rule.

## Auditoría y trazabilidad
- Incluye campos de auditoría en entidades principales:
  - creadoEn
  - modificadoEn
  - creadoPor
  - modificadoPor
- Si procede, prepara soft delete solo en agregados donde tenga sentido funcional.
- No añadas borrado lógico por defecto a todo.

## Rendimiento y consulta
- Diseña entidades pensando en lectura pública frecuente.
- Respuestas REST: DTOs/proyecciones (política de API en `spring-boot-4-backend` y [api-contract.mdc](api-contract.mdc)).
- Para listados, usa proyecciones o DTOs específicos en lugar de cargar agregados completos.
- Para búsquedas con filtros dinámicos, favorece Specification en vez de multiplicar métodos derivados.
- Evita `fetch = EAGER` salvo justificación clara.
- Piensa explícitamente en N+1 queries al definir relaciones.

## Geolocalización y multimedia

### Ubicación (`catalog-service`)

- Modela ubicación de forma explícita y estable.
- En **catalog-service** el `readme` prevé **PostgreSQL + PostGIS**: usa tipos espaciales apropiados (p. ej. `geometry`/`geography` o equivalente JPA/Hibernate) para el punto del ejemplar, no sustituir por simples lat/lon salvo decisión explícita en documentación.
- Solo donde un agregado **no** disponga de PostGIS, latitud y longitud numéricas como campos separados son aceptables.

### Fotografías (`media-service`)

- El agregado **`fotografia`** vive en **`media-service`** (esquema PostgreSQL `media`), enlazado al ejemplar por `ejemplar_id` / `treeId` en API — **no** embeber metadatos de foto en la entidad JPA `Ejemplar` de catálogo ni en Mongo.
- Metadatos mínimos en `media.fotografia`: referencia al ejemplar, `storageKey` / URL derivada, visibilidad, foto principal, categoría, auditoría; binarios en **MinIO** (u otro object storage), nunca en JPA ni Mongo.
- Orquestación cross-service (permiso de subida en catálogo, borrado en cascada al eliminar árbol): ver [media-upload-hu006.md](../../docs/engineering/media-upload-hu006.md) y contrato en [openapi.yaml](../../docs/api/openapi.yaml).
- Al modelar en **`catalog-service`**: como mucho identificadores o flags de negocio acordados por API; la persistencia de fotos es responsabilidad de **`media-service`**.

## Calidad
- Cada entidad debe tener una responsabilidad clara.
- Cada cambio de modelo debe venir acompañado de:
  - migración de esquema,
  - ajuste de DTOs,
  - pruebas de persistencia si aplica.
- Antes de crear una nueva entidad, valida si realmente representa un concepto del negocio.
- Tras cambios de Flyway, entidades JPA o documentos Mongo: revisión estática según [db-postgresql-mtl](../skills/db-postgresql-mtl/SKILL.md); en `catalog-service`, también [db-mongo-mtl](../skills/db-mongo-mtl/SKILL.md) ([canonical-sources.md](../../docs/engineering/canonical-sources.md)).